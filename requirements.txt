pydantic>=2
anthropic
PyGithub
GitPython
docker
pytest
pytest-cov
python-dotenv
graphifyy

# ruff (and other languages' lint tools) run only inside the sandbox containers
# (see sandbox/<language>/Dockerfile) -- not needed in this venv.

# Only needed once the langgraph_adapter is built (Phase 6):
# langgraph
# langgraph-checkpoint-sqlite
