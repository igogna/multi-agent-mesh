# Placeholder — real sandbox image wired up in Phase 2 (sandbox_tools.run_tests/run_lint).
FROM python:3.11-slim

WORKDIR /workspace

# TODO(Phase 2): copy requirements.txt, pip install, run pytest/lint against
# the generated file_changes + test_files mounted at runtime.
